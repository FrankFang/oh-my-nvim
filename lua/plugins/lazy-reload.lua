return {
  "MaximilianLloyd/lazy-reload.nvim",
  opts = {
  },
  keys = {
    -- Opens the command.
    { "<leader>rl", "<cmd>lua require('lazy-reload').feed()<cr>", desc = "Reload a plugin" },
  }
}
